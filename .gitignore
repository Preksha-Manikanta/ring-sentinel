node_modules/
dist/
build/
.cache/
.env*
# …but keep the committed templates (real .env / .env.local stay ignored)
!.env.example
!.env.local.example
.vite/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*

# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/

# Generated dataset (produced by data/generate_dataset.py)
data/accounts.csv
data/orders.csv
data/holdout_labels.json

# Platform-managed agentplat state, not project source. In the
# client-initiated single-repo workspace layout (make's `Path: "."` spec) the
# repo root IS the workspace root, so these land inside this repo rather than
# beside it.
/.plugins/
/.opencode-skills/
